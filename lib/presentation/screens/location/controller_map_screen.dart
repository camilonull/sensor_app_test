import 'package:flutter/material.dart';

class ControllerMapScreen extends StatelessWidget {
  const ControllerMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ControlledMapAcreen'),
      ),
    );
  }
}
